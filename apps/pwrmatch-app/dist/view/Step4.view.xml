<mvc:View controllerName="simulador.controller.Step4" 
          xmlns:mvc="sap.ui.core.mvc" 
          xmlns:m="sap.m"
          xmlns="sap.m">
  <Page title="{i18n>step4Title}">
    <content>
        <!--
      <VBox class="sapUiSmallMargin" justifyContent="Center" alignItems="Center" height="80%">
        <Title text="{i18n>resultTitle}" level="H2" />
        <Button text="{i18n>newSimulation}" press=".onNewSimulation" class="sapUiSmallMarginTop" type="Emphasized" />
        -->
 <VBox class="sapUiSmallMargin">
        <Title text="{i18n>resultTitle}" level="H2" />

        <HBox justifyContent="SpaceBetween" class="sapUiSmallMarginTop">
          <Button text="{i18n>back}" press=".onBack" />
          <Button text="{i18n>newSimulation}" press=".onNewSimulation" class="sapUiSmallMarginTop" type="Emphasized" />
        </HBox>


      </VBox>

    </content>
  </Page>
</mvc:View>
